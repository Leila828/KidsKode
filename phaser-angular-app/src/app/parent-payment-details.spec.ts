import { ParentPaymentDetails } from './parent-payment-details';

describe('ParentPaymentDetails', () => {
  it('should create an instance', () => {
    expect(new ParentPaymentDetails()).toBeTruthy();
  });
});
